# This file is generated by the IceCap build system.
# It is checked-in to version control for convenience and to serve as a reference.

[package]
name = "icecap-backtrace-collect"
version = "0.1.0"
edition = "2018"

[dependencies]
cfg-if = "*"
log = "*"
icecap-backtrace-types = { path = "../types" }

[target."cfg(all(target_os = \"icecap\", icecap_debug))".dependencies]
unwinding = { version = "0.1.4", default-features = false, features = [ "unwinder", "fde-gnu-eh-frame-hdr",] }
icecap-sel4 = { path = "../../icecap-sel4" }
