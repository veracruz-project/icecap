# This file is generated by the IceCap build system.
# It is checked-in to version control for convenience and to serve as a reference.

[package]
name = "hypervisor-serialize-component-config"
version = "0.1.0"
edition = "2018"

[dependencies]
postcard = { version = "*", default-features = false, features = [ "alloc",] }
serde = "*"
serde_json = "*"
hypervisor-benchmark-server-config = { path = "../../benchmark-server/config" }
hypervisor-event-server-config = { path = "../../event-server/config" }
hypervisor-fault-handler-config = { path = "../../fault-handler/config" }
hypervisor-host-vmm-config = { path = "../../vmm/host/config" }
hypervisor-mirage-config = { path = "../../mirage/config" }
hypervisor-realm-vmm-config = { path = "../../vmm/realm/config" }
hypervisor-resource-server-config = { path = "../../resource-server/config" }
hypervisor-serial-server-config = { path = "../../serial-server/config" }
icecap-config-cli-core = { path = "../../../framework/base/icecap-config/cli/core" }
