# This file is generated by the IceCap build system.
# It is checked-in to version control for convenience and to serve as a reference.

[package]
name = "hypervisor-benchmark-server"
version = "0.1.0"
edition = "2018"

[dependencies]
cfg-if = "*"
hypervisor-benchmark-server-config = { path = "./config" }
hypervisor-benchmark-server-types = { path = "./types" }
icecap-plat = { path = "../../framework/base/icecap-plat" }
icecap-std = { path = "../../framework/base/icecap-std" }
