# This file is generated by the IceCap build system.
# It is checked in to version control for the sake of documentation.

[package]
name = "icecap-serial-server-config"
version = "0.1.0"
edition = "2018"

[dependencies]
icecap-config = { path = "../../../icecap/icecap-config" }
icecap-event-server-types = { path = "../../event-server/types" }

[dependencies.serde]
default-features = false
features = [ "alloc", "derive",]
version = "*"
