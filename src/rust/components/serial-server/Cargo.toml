# This file is generated by the IceCap build system.
# It is checked in to version control for the sake of documentation.

[package]
name = "serial-server"
version = "0.1.0"
edition = "2018"

[dependencies]
serde = { version = "*", default-features = false, features = [ "alloc", "derive",] }
tock-registers = "*"
icecap-std = { path = "../../icecap/icecap-std" }
icecap-serial-server-config = { path = "./config" }
icecap-timer-server-client = { path = "../timer-server/client" }
icecap-event-server-types = { path = "../event-server/types" }
