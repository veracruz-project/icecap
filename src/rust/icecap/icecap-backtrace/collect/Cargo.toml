# This file is generated by the IceCap build system.
# It is checked in to version control for the sake of documentation.

[package]
name = "icecap-backtrace-collect"
version = "0.1.0"
edition = "2018"

[dependencies]
icecap-backtrace-types = { path = "../types" }
cfg-if = "*"
log = "*"

[target."cfg(all(target_os = \\\"icecap\\\", icecap_debug))".dependencies]
icecap-unwind = { path = "../../icecap-unwind" }

[target."cfg(all(target_os = \"icecap\", icecap_debug))".dependencies.fallible-iterator]
default-features = false
features = [ "alloc",]
version = "*"

[target."cfg(all(target_os = \"icecap\", icecap_debug))".dependencies.gimli]
default-features = false
features = [ "read",]
version = "0.20.0"
