# This file is generated by the IceCap build system.
# It is checked in to version control for the sake of documentation.

[package]
name = "icecap-sel4"
version = "0.1.0"
edition = "2018"

[features]
default = []
benchmark = []
rustc-dep-of-std = [ "core", "alloc", "compiler_builtins/rustc-dep-of-std", "icecap-sel4-sys/rustc-dep-of-std",]
use-serde = [ "serde",]

[dependencies]
alloc = { version = "1.0.0", optional = true, package = "rustc-std-workspace-alloc" }
compiler_builtins = { version = "0.1.0", optional = true }
core = { version = "1.0.0", optional = true, package = "rustc-std-workspace-core" }
serde = { version = "*", default-features = false, features = [ "alloc", "derive",], optional = true }
icecap-sel4-derive = { path = "./derive" }
icecap-sel4-sys = { path = "./sys" }
